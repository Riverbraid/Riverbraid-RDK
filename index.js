export function verify(i){ const p = i.packet; const s = (i.repo==="Riverbraid-RDK" && i.ring===2 && i.role==="sdk-reference-surface" && p && p.schema==="riverbraid.rdk.packet" && p.version==="1.0.0" && p.operation==="verify" && p.claim_boundary==="deterministic-infrastructure-contract-only"); return {pass:s, stationary:s, signal: s?"rdk:PACKET_STATIONARY":"rdk:DRIFT", reason:s?"Match":"Drift"}; }